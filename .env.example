# PostgreSQL — used by docker-compose db service and API connection string
POSTGRES_USER=langafy
POSTGRES_PASSWORD=langafy_dev
POSTGRES_DB=langafy

# Firebase Authentication (configured in Step 2.7)
FIREBASE_PROJECT_ID=your-firebase-project-id

# Anthropic Claude API (configured in Step 7.1)
CLAUDE_API_KEY=your-claude-api-key

# Web app — public API base URL
NEXT_PUBLIC_API_URL=http://localhost:5000
